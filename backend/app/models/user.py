json
{
  "executor": "Nexora Codex",
  "task": "Generate models/user.py for Nexora SaaS",
  "entity": "user",
  "advice": null,
  "files": [
    {
      "path": "backend/app/models/user.py",
      "content": "# NEXORADOCS: Model definition for the User entity in Nexora SaaS multi-tenant platform.\n# NEXORADOCS: Every user belongs to exactly one tenant; tenant_id is mandatory and enforced at DB level.\n# NEXORADOCS: TenantMixin provides tenant_id column + helper methods for multi-tenant query isolation.\n# NEXORADOCS: Passwords are stored as bcrypt hashes; plain-text storage is strictly forbidden.\n# NEXORADOCS: soft-delete pattern via is_active flag; hard deletes are not allowed for audit trails.\n\nfrom __future__ import annotations\n\nimport uuid\nfrom datetime import datetime, timezone\nfrom typing import Optional\n\nfrom sqlalchemy import (\n    Boolean,\n    Column,\n    DateTime,\n    ForeignKey,\n    Index,\n    String,\n    Text,\n    UniqueConstraint,\n)\nfrom sqlalchemy.dialects.postgresql import UUID\nfrom sqlalchemy.orm import relationship, validates\n\nfrom backend.app.models.base import Base, TenantMixin  # NEXORADOCS: Base declarative base; TenantMixin enforces tenant_id\n\n\nclass User(TenantMixin, Base):\n    \"\"\"NEXORADOCS: Represents an authenticated principal scoped to a single tenant.\n    Business rules:\n      - email must be unique per tenant (not globally), enforced by unique constraint.\n      - username must be unique per tenant.\n      - role determines permission scope: 'owner', 'admin', 'member', 'viewer'.\n      - A user may only be reactivated by a tenant admin or owner.\n      - last_login_at is updated on every successful authentication event.\n    \"\"\"\n\n    __tablename__ = \"users\"\n\n    # ---------------------------------------------------------------------------\n    # Primary key\n    # ---------------------------------------------------------------------------\n    id: uuid.UUID = Column(\n        UUID(as_uuid=True),\n        primary_key=True,\n        default=uuid.uuid4,\n        nullable=False,\n        comment=\"NEXORADOCS: Surrogate PK; UUIDv4 to avoid enumeration attacks.\",\n    )\n\n    # ---------------------------------------------------------------------------\n    # tenant_id is inherited from TenantMixin as a required ForeignKey column.\n    # NEXORADOCS: Never query User without filtering by tenant_id.\n    # ---------------------------------------------------------------------------\n\n    # ---------------------------------------------------------------------------\n    # Identity fields\n    # ---------------------------------------------------------------------------\n    email: str = Column(\n        String(320),\n        nullable=False,\n        index=True,\n        comment=\"NEXORADOCS: RFC 5321 max length 320; unique per tenant enforced by UniqueConstraint below.\",\n    )\n\n    username: str = Column(\n        String(64),\n        nullable=False,\n        index=True,\n        comment=\"NEXORADOCS: URL-safe handle; unique per tenant.\",\n    )\n\n    full_name: Optional[str] = Column(\n        String(256),\n        nullable=True,\n        comment=\"NEXORADOCS: Display name; optional but encouraged for UX.\",\n    )\n\n    # ---------------------------------------------------------------------------\n    # Authentication\n    # ---------------------------------------------------------------------------\n    hashed_password: str = Column(\n        Text,\n        nullable=False,\n        comment=\"NEXORADOCS: bcrypt hash only; plain-text MUST never be stored.\",\n    )\n\n    # ---------------------------------------------------------------------------\n    # Authorization\n    # ---------------------------------------------------------------------------\n    role: str = Column(\n        String(32),\n        nullable=False,\n        default=\"member\",\n        comment=\"NEXORADOCS: Allowed values: owner | admin | member | viewer. Enforced at service layer.\",\n    )\n\n    # ---------------------------------------------------------------------------\n    # Status flags\n    # ---------------------------------------------------------------------------\n    is_active: bool = Column(\n        Boolean,\n        nullable=False,\n        default=True,\n        comment=\"NEXORADOCS: Soft-delete flag; inactive users cannot authenticate.\",\n    )\n\n    is_verified: bool = Column(\n        Boolean,\n        nullable=False,\n        default=False,\n        comment=\"NEXORADOCS: Email verification gate; unverified users have read-only access.\",\n    )\n\n    is_superuser: bool = Column(\n        Boolean,\n        nullable=False,\n        default=False,\n        comment=\"NEXORADOCS: Cross-tenant superuser flag; used exclusively for platform operators.\",\n    )\n\n    # ---------------------------------------------------------------------------\n    # Timestamps\n    # ---------------------------------------------------------------------------\n    created_at: datetime = Column(\n        DateTime(timezone=True),\n        nullable=False,\n        default=lambda: datetime.now(timezone.utc),\n        comment=\"NEXORADOCS: Immutable creation timestamp in UTC.\",\n    )\n\n    updated_at: datetime = Column(\n        DateTime(timezone=True),\n        nullable=False,\n        default=lambda: datetime.now(timezone.utc),\n        onupdate=lambda: datetime.now(timezone.utc),\n        comment=\"NEXORADOCS: Auto-updated on every write; used for optimistic locking checks.\",\n    )\n\n    last_login_at: Optional[datetime] = Column(\n        DateTime(timezone=True),\n        nullable=True,\n        comment=\"NEXORADOCS: Updated by AuthService on successful token issuance.\",\n    )\n\n    # ---------------------------------------------------------------------------\n    # Optional profile extras\n    # ---------------------------------------------------------------------------\n    avatar_url: Optional[str] = Column(\n        Text,\n        nullable=True,\n        comment=\"NEXORADOCS: Absolute URL to avatar; CDN path managed by StorageService.\",\n    )\n\n    locale: str = Column(\n        String(16),\n        nullable=False,\n        default=\"en\",\n        comment=\"NEXORADOCS: BCP-47 locale code; drives i18n formatting.\",\n    )\n\n    timezone_name: str = Column(\n        String(64),\n        nullable=False,\n        default=\"UTC\",\n        comment=\"NEXORADOCS: IANA timezone identifier; drives datetime rendering.\",\n    )\n\n    # ---------------------------------------------------------------------------\n    # Relationships\n    # ---------------------------------------------------------------------------\n    # NEXORADOCS: back_populates counterpart defined on Tenant model.\n    tenant = relationship(\n        \"Tenant\",\n        back_populates=\"users\",\n        lazy=\"select\",\n        foreign_keys=\"[User.tenant_id]\",\n    )\n\n    # NEXORADOCS: A user may hold multiple active API keys scoped to their tenant.\n    api_keys = relationship(\n        \"ApiKey\",\n        back_populates=\"user\",\n        cascade=\"all, delete-orphan\",\n        lazy=\"dynamic\",\n        primaryjoin=\"and_(ApiKey.user_id == User.id, ApiKey.tenant_id == User.tenant_id)\",\n    )\n\n    # ---------------------------------------------------------------------------\n    # Table-level constraints and indexes\n    # ---------------------------------------------------------------------------\n    __table_args__ = (\n        # NEXORADOCS: email uniqueness is per-tenant, not global.\n        UniqueConstraint(\"tenant_id\", \"email\", name=\"uq_users_tenant_email\"),\n        # NEXORADOCS: username uniqueness is per-tenant, not global.\n        UniqueConstraint(\"tenant_id\", \"username\", name=\"uq_users_tenant_username\"),\n        # NEXORADOCS: composite index for the most frequent query pattern: tenant + active + role.\n        Index(\"ix_users_tenant_active_role\", \"tenant_id\", \"is_active\", \"role\"),\n        # NEXORADOCS: index to accelerate last-login reporting queries.\n        Index(\"ix_users_tenant_last_login\", \"tenant_id\", \"last_login_at\"),\n        {\"schema\": None},  # NEXORADOCS: schema override point for multi-schema deployments.\n    )\n\n    # ---------------------------------------------------------------------------\n    # Validators\n    # ---------------------------------------------------------------------------\n    @validates(\"role\")\n    def validate_role(self, key: str, value: str) -> str:\n        # NEXORADOCS: Whitelist of allowed roles; raise early to avoid silent corruption.\n        allowed_roles = {\"owner\", \"admin\", \"member\", \"viewer\"}\n        if value not in allowed_roles:\n            raise ValueError(\n                f\"NEXORADOCS: Invalid role '{value}'. Must be one of {sorted(allowed_roles)}.\"\n            )\n        return value\n\n    @validates(\"email\")\n    def validate_email(self, key: str, value: str) -> str:\n        # NEXORADOCS: Basic structural guard; full RFC validation delegated to Pydantic schema.\n        if not value or \"@\" not in value:\n            raise ValueError(\"NEXORADOCS: Email must contain '@' character.\")\n        return value.lower().strip()\n\n    @validates(\"username\")\n    def validate_username(self, key: str, value: str) -> str:\n        # NEXORADOCS: Enforce slug-safe characters; prevents XSS vectors in URL paths.\n        import re\n        if not re.match(r\"^[a-zA-Z0-9_.-]{3,64}$\", value):\n            raise ValueError(\n                \"NEXORADOCS: Username must be 3-64 chars, alphanumeric, dots, hyphens or underscores.\"\n            )\n        return value.strip()\n\n    # ---------------------------------------------------------------------------\n    # Dunder helpers\n    # ---------------------------------------------------------------------------\n    def __repr__(self) -> str:  # NEXORADOCS: Safe repr; never exposes hashed_password.\n        return (\n            f\"<User id={self.id} tenant_id={self.tenant_id} \"\n            f\"username={self.username!r} role={self.role!r} active={self.is_active}>\"\n        )\n\n    # ---------------------------------------------------------------------------\n    # Business-logic helpers (pure; no DB I/O)\n    # ---------------------------------------------------------------------------\n    def is_owner(self) -> bool:\n        # NEXORADOCS: Convenience predicate used in permission guards.\n        return self.role == \"owner\"\n\n    def is_admin_or_above(self) -> bool:\n        # NEXORADOCS: Returns True for roles with write-level tenant access.\n        return self.role in {\"owner\", \"admin\"}\n\n    def can_manage_users(self) -> bool:\n        # NEXORADOCS: Only owners and admins may invite or deactivate other users.\n        return self.is_active and self.is_admin_or_above()\n\n    def mark_verified(self) -> None:\n        # NEXORADOCS: Called by VerificationService after token validation; does not flush.\n        self.is_verified = True\n        self.updated_at = datetime.now(timezone.utc)\n\n    def deactivate(self) -> None:\n        # NEXORADOCS: Soft-delete; preserves audit trail. Caller must commit the session.\n        self.is_active = False\n        self.updated_at = datetime.now(timezone.utc)\n\n    def record_login(self) -> None:\n        # NEXORADOCS: Called by AuthService immediately after token issuance.\n        self.last_login_at = datetime.now(timezone.utc)\n        self.updated_at = datetime.now(timezone.utc)\n"
    },
    {
      "path": "backend/app/models/base.py",
      "content": "# NEXORADOCS: Declarative base and TenantMixin shared across all Nexora SaaS models.\n# NEXORADOCS: TenantMixin injects tenant_id as a non-nullable FK + helper for tenant-scoped queries.\n# NEXORADOCS: All models must extend TenantMixin before Base to guarantee tenant isolation.\n\nfrom __future__ import annotations\n\nimport uuid\nfrom typing import Any\n\nfrom sqlalchemy import Column, ForeignKey, text\nfrom sqlalchemy.dialects.postgresql import UUID\nfrom sqlalchemy.orm import DeclarativeBase, declared_attr, Query\n\n\nclass Base(DeclarativeBase):\n    \"\"\"NEXORADOCS: Project-wide SQLAlchemy declarative base.\"\"\"\n\n    @declared_attr.directive\n    def __tablename__(cls) -> str:  # noqa: N805\n        # NEXORADOCS: Auto-derive table name from class name; override per model when needed.\n        return cls.__name__.lower() + \"s\"\n\n\nclass TenantMixin:\n    \"\"\"NEXORADOCS: Mixin that injects a mandatory tenant_id foreign-key column.\n    Usage: class MyModel(TenantMixin, Base): ...\n    Rules:\n      - tenant_id is non-nullable; every INSERT must supply it.\n      - Index on tenant_id is created automatically for every inheriting table.\n      - The by_tenant class-method is the ONLY sanctioned way to scope queries.\n    \"\"\"\n\n    @declared_attr\n    def tenant_id(cls) -> Column:  # noqa: N805\n        # NEXORADOCS: FK references the tenants table; ON DELETE RESTRICT to prevent orphan rows.\n        return Column(\n            UUID(as_uuid=True),\n            ForeignKey(\"tenants.id\", ondelete=\"RESTRICT\"),\n            nullable=False,\n            index=True,\n            comment=\"NEXORADOCS: Mandatory tenant scope. Never query without this filter.\",\n        )\n\n    @classmethod\n    def by_tenant(cls, session: Any, tenant_id: uuid.UUID) -> Query:  # type: ignore[return]\n        # NEXORADOCS: Canonical entry-point for all tenant-scoped reads.\n        # NEXORADOCS: Callers MUST use this method instead of raw session.query to prevent data leakage.\n        if tenant_id is None:\n            raise ValueError(\"NEXORADOCS: tenant_id is required for all data access operations.\")\n        return session.query(cls).filter(cls.tenant_id == tenant_id)\n"
    },
    {
      "path": "backend/app/models/__init__.py",
      "content": "# NEXORADOCS: Package init; imports all models so Alembic autogenerate detects them.\nfrom backend.app.models.base import Base, TenantMixin  # noqa: F401\nfrom backend.app.models.user import User  # noqa: F401\n\n__all__ = [\"Base\", \"TenantMixin\", \"User\"]\n"
    }
  ]
}