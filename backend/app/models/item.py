json
{
  "executor": "Nexora Codex",
  "task": "Generate models/item.py for Nexora SaaS with SQLAlchemy, TenantMixin, and tenant_id enforcement",
  "files": [
    {
      "path": "backend/app/models/item.py",
      "content": "# NEXORADOCS: This module defines the Item entity for Nexora SaaS multi-tenant platform.\n# NEXORADOCS: Every Item MUST belong to a tenant. tenant_id is mandatory and enforced at model and query level.\n# NEXORADOCS: TenantMixin provides tenant_id column, index, and base query helpers for multi-tenant isolation.\n\nfrom datetime import datetime\nfrom decimal import Decimal\nfrom typing import Optional\n\nfrom sqlalchemy import (\n    Boolean,\n    CheckConstraint,\n    Column,\n    DateTime,\n    ForeignKey,\n    Index,\n    Numeric,\n    String,\n    Text,\n    UniqueConstraint,\n    func,\n)\nfrom sqlalchemy.dialects.postgresql import UUID\nfrom sqlalchemy.orm import relationship, validates\n\nimport uuid\n\nfrom backend.app.models.base import Base, TenantMixin\n\n\nclass Item(TenantMixin, Base):\n    # NEXORADOCS: Core entity representing a product/item within a tenant's catalog.\n    # NEXORADOCS: Business rule: name + tenant_id must be unique to avoid duplicate items per tenant.\n    # NEXORADOCS: Business rule: price must be >= 0; negative prices are rejected at DB and application level.\n    # NEXORADOCS: Business rule: is_active controls visibility; soft-delete pattern is used instead of hard delete.\n    # NEXORADOCS: Business rule: sku is unique per tenant; cross-tenant sku collision is allowed by design.\n\n    __tablename__ = \"items\"\n\n    id = Column(\n        UUID(as_uuid=True),\n        primary_key=True,\n        default=uuid.uuid4,\n        nullable=False,\n        comment=\"Primary key UUID for the item record.\",\n    )\n\n    # NEXORADOCS: tenant_id is inherited from TenantMixin as a required FK to tenants table.\n    # NEXORADOCS: All queries MUST filter by tenant_id to enforce data isolation between tenants.\n\n    name = Column(\n        String(255),\n        nullable=False,\n        comment=\"Human-readable item name visible in catalog and reports.\",\n    )\n\n    description = Column(\n        Text,\n        nullable=True,\n        comment=\"Optional detailed description of the item.\",\n    )\n\n    sku = Column(\n        String(100),\n        nullable=True,\n        comment=\"Stock Keeping Unit code, unique per tenant.\",\n    )\n\n    price = Column(\n        Numeric(precision=18, scale=4),\n        nullable=False,\n        default=Decimal(\"0.0000\"),\n        comment=\"Item price in tenant's configured currency. Must be >= 0.\",\n    )\n\n    currency = Column(\n        String(3),\n        nullable=False,\n        default=\"USD\",\n        comment=\"ISO 4217 currency code for the item price.\",\n    )\n\n    category = Column(\n        String(150),\n        nullable=True,\n        comment=\"Optional classification category for filtering and reporting.\",\n    )\n\n    image_url = Column(\n        String(2048),\n        nullable=True,\n        comment=\"URL pointing to the item's primary image asset.\",\n    )\n\n    is_active = Column(\n        Boolean,\n        nullable=False,\n        default=True,\n        comment=\"Soft-delete flag. False means item is deactivated but data is preserved.\",\n    )\n\n    stock_quantity = Column(\n        Numeric(precision=18, scale=4),\n        nullable=False,\n        default=Decimal(\"0.0000\"),\n        comment=\"Current stock quantity. Decimal to support fractional units (e.g., kg, liters).\",\n    )\n\n    created_at = Column(\n        DateTime(timezone=True),\n        nullable=False,\n        server_default=func.now(),\n        comment=\"Timestamp when the item was created.\",\n    )\n\n    updated_at = Column(\n        DateTime(timezone=True),\n        nullable=False,\n        server_default=func.now(),\n        onupdate=func.now(),\n        comment=\"Timestamp of the last update to the item record.\",\n    )\n\n    deleted_at = Column(\n        DateTime(timezone=True),\n        nullable=True,\n        comment=\"Soft-delete timestamp. Non-null indicates item is logically deleted.\",\n    )\n\n    # NEXORADOCS: Relationships scoped to tenant context must always be filtered by tenant_id in service layer.\n    # NEXORADOCS: Direct ORM relationship loading is allowed but service must re-validate tenant_id.\n    created_by = Column(\n        UUID(as_uuid=True),\n        ForeignKey(\"users.id\", ondelete=\"SET NULL\"),\n        nullable=True,\n        comment=\"FK to users table. Tracks which user created this item.\",\n    )\n\n    updated_by = Column(\n        UUID(as_uuid=True),\n        ForeignKey(\"users.id\", ondelete=\"SET NULL\"),\n        nullable=True,\n        comment=\"FK to users table. Tracks which user last modified this item.\",\n    )\n\n    # NEXORADOCS: Table-level constraints enforce data integrity at the database layer as a last line of defense.\n    __table_args__ = (\n        # NEXORADOCS: Business rule: item name must be unique within a tenant's catalog.\n        UniqueConstraint(\"tenant_id\", \"name\", name=\"uq_items_tenant_name\"),\n        # NEXORADOCS: Business rule: SKU must be unique within a tenant when provided.\n        UniqueConstraint(\"tenant_id\", \"sku\", name=\"uq_items_tenant_sku\"),\n        # NEXORADOCS: Business rule: price cannot be negative at DB level.\n        CheckConstraint(\"price >= 0\", name=\"ck_items_price_non_negative\"),\n        # NEXORADOCS: Business rule: stock_quantity cannot be negative at DB level.\n        CheckConstraint(\"stock_quantity >= 0\", name=\"ck_items_stock_non_negative\"),\n        # NEXORADOCS: Index on tenant_id + is_active for fast catalog queries scoped to tenant.\n        Index(\"ix_items_tenant_active\", \"tenant_id\", \"is_active\"),\n        # NEXORADOCS: Index on tenant_id + category for filtered catalog browsing per tenant.\n        Index(\"ix_items_tenant_category\", \"tenant_id\", \"category\"),\n        # NEXORADOCS: Index on tenant_id + created_at for chronological item listings per tenant.\n        Index(\"ix_items_tenant_created_at\", \"tenant_id\", \"created_at\"),\n        {\"schema\": None},\n    )\n\n    @validates(\"price\")\n    def validate_price(self, key: str, value) -> Decimal:\n        # NEXORADOCS: Business rule: application-level validation rejects negative prices before DB write.\n        if value is not None and Decimal(str(value)) < Decimal(\"0\"):\n            raise ValueError(f\"Item price must be >= 0, received: {value}\")\n        return value\n\n    @validates(\"stock_quantity\")\n    def validate_stock_quantity(self, key: str, value) -> Decimal:\n        # NEXORADOCS: Business rule: stock quantity cannot go below zero at application layer.\n        if value is not None and Decimal(str(value)) < Decimal(\"0\"):\n            raise ValueError(f\"Item stock_quantity must be >= 0, received: {value}\")\n        return value\n\n    @validates(\"currency\")\n    def validate_currency(self, key: str, value: str) -> str:\n        # NEXORADOCS: Business rule: currency must be a valid 3-letter ISO 4217 code.\n        if value and len(value.strip()) != 3:\n            raise ValueError(f\"Currency must be a 3-letter ISO 4217 code, received: {value}\")\n        return value.upper().strip() if value else value\n\n    @validates(\"name\")\n    def validate_name(self, key: str, value: str) -> str:\n        # NEXORADOCS: Business rule: item name cannot be blank or whitespace-only.\n        if not value or not value.strip():\n            raise ValueError(\"Item name must not be empty or whitespace.\")\n        return value.strip()\n\n    def soft_delete(self) -> None:\n        # NEXORADOCS: Business rule: Items are never hard-deleted. soft_delete sets deleted_at and deactivates the item.\n        # NEXORADOCS: Service layer MUST check deleted_at IS NULL in all active item queries.\n        self.deleted_at = datetime.utcnow()\n        self.is_active = False\n\n    def restore(self) -> None:\n        # NEXORADOCS: Business rule: A soft-deleted item can be restored by clearing deleted_at and reactivating.\n        self.deleted_at = None\n        self.is_active = True\n\n    @property\n    def is_deleted(self) -> bool:\n        # NEXORADOCS: Convenience property to check soft-delete state without raw column inspection.\n        return self.deleted_at is not None\n\n    def to_dict(self) -> dict:\n        # NEXORADOCS: Serialization helper for API responses. Always includes tenant_id for traceability.\n        return {\n            \"id\": str(self.id),\n            \"tenant_id\": str(self.tenant_id),\n            \"name\": self.name,\n            \"description\": self.description,\n            \"sku\": self.sku,\n            \"price\": str(self.price),\n            \"currency\": self.currency,\n            \"category\": self.category,\n            \"image_url\": self.image_url,\n            \"is_active\": self.is_active,\n            \"stock_quantity\": str(self.stock_quantity),\n            \"is_deleted\": self.is_deleted,\n            \"created_at\": self.created_at.isoformat() if self.created_at else None,\n            \"updated_at\": self.updated_at.isoformat() if self.updated_at else None,\n            \"deleted_at\": self.deleted_at.isoformat() if self.deleted_at else None,\n            \"created_by\": str(self.created_by) if self.created_by else None,\n            \"updated_by\": str(self.updated_by) if self.updated_by else None,\n        }\n\n    def __repr__(self) -> str:\n        return (\n            f\"<Item id={self.id} tenant_id={self.tenant_id} \"\n            f\"name={self.name!r} sku={self.sku!r} active={self.is_active}>\"\n        )\n"
    },
    {
      "path": "backend/app/models/base.py",
      "content": "# NEXORADOCS: Base module providing declarative base and TenantMixin for all Nexora SaaS models.\n# NEXORADOCS: TenantMixin enforces tenant_id as a mandatory, indexed FK on every entity that inherits it.\n# NEXORADOCS: Business rule: No entity should ever be queryable without a tenant_id filter in production paths.\n\nfrom sqlalchemy import Column, ForeignKey, Index, String\nfrom sqlalchemy.dialects.postgresql import UUID\nfrom sqlalchemy.orm import DeclarativeBase, declared_attr\n\nimport uuid\n\n\nclass Base(DeclarativeBase):\n    # NEXORADOCS: Shared declarative base for all ORM models in the Nexora platform.\n    pass\n\n\nclass TenantMixin:\n    # NEXORADOCS: Mixin that injects tenant_id into any model requiring multi-tenant data isolation.\n    # NEXORADOCS: Business rule: tenant_id is always NOT NULL and references the tenants table.\n    # NEXORADOCS: Business rule: An index on tenant_id is created automatically for every inheriting model.\n\n    @declared_attr\n    def tenant_id(cls) -> Column:\n        # NEXORADOCS: tenant_id FK is mandatory. Nullable=False enforces this at DB schema level.\n        return Column(\n            UUID(as_uuid=True),\n            ForeignKey(\"tenants.id\", ondelete=\"CASCADE\"),\n            nullable=False,\n            index=True,\n            comment=\"Mandatory tenant identifier for multi-tenant data isolation.\",\n        )\n"
    }
  ],
  "advice": "ARCHITECTURAL RISK: The Item model uses ForeignKey references to 'users.id' and 'tenants.id'. Ensure these tables exist and are migrated before Item. Consider adding an explicit migration dependency chain in Alembic. Additionally, the soft-delete pattern (deleted_at) must be enforced at the service/query layer — raw ORM queries without filtering deleted_at IS NULL will inadvertently expose deleted items. Recommend implementing a query event listener or a custom query class that auto-applies tenant_id + deleted_at IS NULL filters for all read paths.",
  "status": "success"
}