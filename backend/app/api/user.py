json
{
  "thoughts": "Creating a complete, production-ready FastAPI router for the 'user' entity with full CRUD, Pydantic schemas, TenantMixin enforcement, and tenant_id isolation on every data access path.",
  "actions": [
    {
      "type": "create_file",
      "path": "backend/app/api/user.py",
      "content": "# NEXORADOCS: API layer for User entity in Nexora SaaS.\n# NEXORADOCS: All routes enforce tenant_id isolation via TenantMixin and query filters.\n# NEXORADOCS: Business rule - tenant_id is mandatory on every create/read/update/delete path.\n\nfrom fastapi import APIRouter, Depends, HTTPException, status, Query\nfrom pydantic import BaseModel, EmailStr, Field, validator\nfrom typing import Optional, List\nfrom uuid import UUID, uuid4\nfrom datetime import datetime\n\nfrom backend.app.services.user_service import UserService, get_user_service\nfrom backend.app.models.user import UserRole\n\nrouter = APIRouter(\n    prefix=\"/users\",\n    tags=[\"users\"],\n)\n\n\n# ---------------------------------------------------------------------------\n# NEXORADOCS: Request schemas — tenant_id is required on all write operations.\n# ---------------------------------------------------------------------------\n\nclass UserCreateRequest(BaseModel):\n    tenant_id: UUID = Field(..., description=\"Tenant this user belongs to\")\n    email: EmailStr = Field(..., description=\"Unique email within the tenant scope\")\n    full_name: str = Field(..., min_length=2, max_length=255)\n    role: UserRole = Field(default=UserRole.MEMBER)\n    password: str = Field(..., min_length=8, max_length=128)\n    is_active: bool = Field(default=True)\n\n    @validator(\"full_name\")\n    def strip_full_name(cls, v: str) -> str:\n        # NEXORADOCS: Trim whitespace to avoid duplicate-looking names.\n        return v.strip()\n\n\nclass UserUpdateRequest(BaseModel):\n    tenant_id: UUID = Field(..., description=\"Tenant this user belongs to — used as isolation key\")\n    email: Optional[EmailStr] = None\n    full_name: Optional[str] = Field(None, min_length=2, max_length=255)\n    role: Optional[UserRole] = None\n    password: Optional[str] = Field(None, min_length=8, max_length=128)\n    is_active: Optional[bool] = None\n\n    @validator(\"full_name\")\n    def strip_full_name(cls, v: Optional[str]) -> Optional[str]:\n        return v.strip() if v else v\n\n\n# ---------------------------------------------------------------------------\n# NEXORADOCS: Response schemas — never expose hashed passwords.\n# ---------------------------------------------------------------------------\n\nclass UserResponse(BaseModel):\n    id: UUID\n    tenant_id: UUID\n    email: EmailStr\n    full_name: str\n    role: UserRole\n    is_active: bool\n    created_at: datetime\n    updated_at: datetime\n\n    class Config:\n        orm_mode = True\n        use_enum_values = True\n\n\nclass UserListResponse(BaseModel):\n    total: int\n    items: List[UserResponse]\n\n\n# ---------------------------------------------------------------------------\n# NEXORADOCS: Routes — every handler propagates tenant_id down to the service layer.\n# ---------------------------------------------------------------------------\n\n@router.post(\n    \"\",\n    response_model=UserResponse,\n    status_code=status.HTTP_201_CREATED,\n    summary=\"Create a new user inside a tenant\",\n)\nasync def create_user(\n    payload: UserCreateRequest,\n    service: UserService = Depends(get_user_service),\n) -> UserResponse:\n    # NEXORADOCS: tenant_id comes from the validated payload; service enforces uniqueness per tenant.\n    user = await service.create_user(\n        tenant_id=payload.tenant_id,\n        email=payload.email,\n        full_name=payload.full_name,\n        role=payload.role,\n        password=payload.password,\n        is_active=payload.is_active,\n    )\n    return UserResponse.from_orm(user)\n\n\n@router.get(\n    \"\",\n    response_model=UserListResponse,\n    status_code=status.HTTP_200_OK,\n    summary=\"List all users for a given tenant\",\n)\nasync def list_users(\n    tenant_id: UUID = Query(..., description=\"Filter users strictly by tenant\"),\n    skip: int = Query(0, ge=0),\n    limit: int = Query(50, ge=1, le=500),\n    is_active: Optional[bool] = Query(None),\n    service: UserService = Depends(get_user_service),\n) -> UserListResponse:\n    # NEXORADOCS: tenant_id is a required query param — rejects requests without it at the schema level.\n    users, total = await service.list_users(\n        tenant_id=tenant_id,\n        skip=skip,\n        limit=limit,\n        is_active=is_active,\n    )\n    return UserListResponse(\n        total=total,\n        items=[UserResponse.from_orm(u) for u in users],\n    )\n\n\n@router.get(\n    \"/{user_id}\",\n    response_model=UserResponse,\n    status_code=status.HTTP_200_OK,\n    summary=\"Retrieve a single user by ID, scoped to a tenant\",\n)\nasync def get_user(\n    user_id: UUID,\n    tenant_id: UUID = Query(..., description=\"Tenant scope — prevents cross-tenant data leaks\"),\n    service: UserService = Depends(get_user_service),\n) -> UserResponse:\n    # NEXORADOCS: Both user_id AND tenant_id are used in the DB filter to prevent cross-tenant reads.\n    user = await service.get_user_by_id(tenant_id=tenant_id, user_id=user_id)\n    if user is None:\n        raise HTTPException(\n            status_code=status.HTTP_404_NOT_FOUND,\n            detail=f\"User '{user_id}' not found in tenant '{tenant_id}'.\",\n        )\n    return UserResponse.from_orm(user)\n\n\n@router.put(\n    \"/{user_id}\",\n    response_model=UserResponse,\n    status_code=status.HTTP_200_OK,\n    summary=\"Update an existing user, scoped to a tenant\",\n)\nasync def update_user(\n    user_id: UUID,\n    payload: UserUpdateRequest,\n    service: UserService = Depends(get_user_service),\n) -> UserResponse:\n    # NEXORADOCS: tenant_id from payload is cross-checked against the record in the service layer.\n    user = await service.update_user(\n        tenant_id=payload.tenant_id,\n        user_id=user_id,\n        email=payload.email,\n        full_name=payload.full_name,\n        role=payload.role,\n        password=payload.password,\n        is_active=payload.is_active,\n    )\n    if user is None:\n        raise HTTPException(\n            status_code=status.HTTP_404_NOT_FOUND,\n            detail=f\"User '{user_id}' not found in tenant '{payload.tenant_id}'.\",\n        )\n    return UserResponse.from_orm(user)\n\n\n@router.delete(\n    \"/{user_id}\",\n    status_code=status.HTTP_204_NO_CONTENT,\n    summary=\"Soft-delete a user, scoped to a tenant\",\n)\nasync def delete_user(\n    user_id: UUID,\n    tenant_id: UUID = Query(..., description=\"Tenant scope — prevents cross-tenant deletions\"),\n    service: UserService = Depends(get_user_service),\n) -> None:\n    # NEXORADOCS: Soft-delete keeps audit trail; tenant_id prevents deleting another tenant's user.\n    deleted = await service.delete_user(tenant_id=tenant_id, user_id=user_id)\n    if not deleted:\n        raise HTTPException(\n            status_code=status.HTTP_404_NOT_FOUND,\n            detail=f\"User '{user_id}' not found in tenant '{tenant_id}'.\",\n        )\n"
    },
    {
      "type": "create_file",
      "path": "backend/app/models/user.py",
      "content": "# NEXORADOCS: ORM model for User entity.\n# NEXORADOCS: TenantMixin injects tenant_id column + composite unique constraints per tenant.\n# NEXORADOCS: Business rule — email uniqueness is scoped per tenant, not globally.\n\nimport enum\nfrom datetime import datetime\nfrom uuid import uuid4\n\nfrom sqlalchemy import (\n    Column,\n    String,\n    Boolean,\n    DateTime,\n    Enum as SAEnum,\n    UniqueConstraint,\n    Index,\n)\nfrom sqlalchemy.dialects.postgresql import UUID\nfrom sqlalchemy.orm import validates\n\nfrom backend.app.models.base import Base, TenantMixin\n\n\nclass UserRole(str, enum.Enum):\n    OWNER = \"owner\"\n    ADMIN = \"admin\"\n    MEMBER = \"member\"\n    VIEWER = \"viewer\"\n\n\nclass User(TenantMixin, Base):\n    # NEXORADOCS: TenantMixin provides tenant_id FK + helper methods for tenant-scoped queries.\n    __tablename__ = \"users\"\n\n    id = Column(\n        UUID(as_uuid=True),\n        primary_key=True,\n        default=uuid4,\n        nullable=False,\n        index=True,\n    )\n    email = Column(String(255), nullable=False)\n    full_name = Column(String(255), nullable=False)\n    hashed_password = Column(String(255), nullable=False)\n    role = Column(\n        SAEnum(UserRole, name=\"user_role_enum\"),\n        nullable=False,\n        default=UserRole.MEMBER,\n    )\n    is_active = Column(Boolean, nullable=False, default=True)\n    is_deleted = Column(Boolean, nullable=False, default=False)  # NEXORADOCS: Soft-delete flag.\n    created_at = Column(DateTime, nullable=False, default=datetime.utcnow)\n    updated_at = Column(\n        DateTime,\n        nullable=False,\n        default=datetime.utcnow,\n        onupdate=datetime.utcnow,\n    )\n\n    __table_args__ = (\n        # NEXORADOCS: Email must be unique per tenant — different tenants can share the same email.\n        UniqueConstraint(\"tenant_id\", \"email\", name=\"uq_users_tenant_email\"),\n        Index(\"ix_users_tenant_id\", \"tenant_id\"),\n        Index(\"ix_users_tenant_email\", \"tenant_id\", \"email\"),\n        Index(\"ix_users_tenant_active\", \"tenant_id\", \"is_active\", \"is_deleted\"),\n    )\n\n    @validates(\"email\")\n    def lowercase_email(self, key: str, value: str) -> str:\n        # NEXORADOCS: Normalize email to lowercase before persistence.\n        return value.lower().strip() if value else value\n\n    def __repr__(self) -> str:\n        return f\"<User id={self.id} tenant_id={self.tenant_id} email={self.email}>\"\n"
    },
    {
      "type": "create_file",
      "path": "backend/app/models/base.py",
      "content": "# NEXORADOCS: Declarative base and TenantMixin shared across all ORM models.\n# NEXORADOCS: TenantMixin adds tenant_id as a non-nullable UUID FK to the tenants table.\n# NEXORADOCS: Business rule — every entity that extends TenantMixin MUST filter by tenant_id.\n\nfrom uuid import uuid4\nfrom sqlalchemy import Column, ForeignKey\nfrom sqlalchemy.dialects.postgresql import UUID\nfrom sqlalchemy.orm import DeclarativeMeta, declarative_base\n\nBase: DeclarativeMeta = declarative_base()\n\n\nclass TenantMixin:\n    # NEXORADOCS: tenant_id references the tenants table; cascade deletes are intentionally omitted\n    # to prevent accidental data loss — use a background job for tenant off-boarding.\n    tenant_id = Column(\n        UUID(as_uuid=True),\n        ForeignKey(\"tenants.id\", ondelete=\"RESTRICT\"),\n        nullable=False,\n        index=True,\n    )\n"
    },
    {
      "type": "create_file",
      "path": "backend/app/services/user_service.py",
      "content": "# NEXORADOCS: Service layer for User entity — all business logic and data access lives here.\n# NEXORADOCS: Every public method receives tenant_id and applies it to every query filter.\n# NEXORADOCS: Business rules:\n#   - Passwords are hashed with bcrypt before storage.\n#   - Deletion is soft-only (is_deleted=True); callers receive a bool indicating success.\n#   - Email uniqueness is enforced per tenant at the DB level (UniqueConstraint) and here.\n\nfrom __future__ import annotations\n\nfrom typing import Optional, Tuple, List\nfrom uuid import UUID\n\nfrom fastapi import Depends\nfrom sqlalchemy import select, func, and_, update as sa_update\nfrom sqlalchemy.ext.asyncio import AsyncSession\nfrom passlib.context import CryptContext\n\nfrom backend.app.models.user import User, UserRole\nfrom backend.app.db.session import get_async_session\n\n_pwd_context = CryptContext(schemes=[\"bcrypt\"], deprecated=\"auto\")\n\n\nclass UserService:\n    # NEXORADOCS: All DB operations use async SQLAlchemy sessions for non-blocking I/O.\n\n    def __init__(self, session: AsyncSession) -> None:\n        self._session = session\n\n    # ------------------------------------------------------------------\n    # NEXORADOCS: Internal helpers\n    # ------------------------------------------------------------------\n\n    def _hash_password(self, plain: str) -> str:\n        # NEXORADOCS: Never store plain-text passwords.\n        return _pwd_context.hash(plain)\n\n    def _base_query(self, tenant_id: UUID):\n        # NEXORADOCS: Every query uses this as its base to guarantee tenant isolation.\n        return (\n            select(User)\n            .where(\n                and_(\n                    User.tenant_id == tenant_id,\n                    User.is_deleted.is_(False),\n                )\n            )\n        )\n\n    # ------------------------------------------------------------------\n    # NEXORADOCS: Public CRUD methods\n    # ------------------------------------------------------------------\n\n    async def create_user(\n        self,\n        *,\n        tenant_id: UUID,\n        email: str,\n        full_name: str,\n        role: UserRole,\n        password: str,\n        is_active: bool,\n    ) -> User:\n        # NEXORADOCS: Check for existing active user with same email within tenant.\n        existing = await self._session.execute(\n            self._base_query(tenant_id).where(User.email == email.lower().strip())\n        )\n        if existing.scalars().first() is not None:\n            from fastapi import HTTPException, status\n            raise HTTPException(\n                status_code=status.HTTP_409_CONFLICT,\n                detail=f\"Email '{email}' is already registered in this tenant.\",\n            )\n\n        user = User(\n            tenant_id=tenant_id,\n            email=email,\n            full_name=full_name,\n            role=role,\n            hashed_password=self._hash_password(password),\n            is_active=is_active,\n        )\n        self._session.add(user)\n        await self._session.commit()\n        await self._session.refresh(user)\n        return user\n\n    async def list_users(\n        self,\n        *,\n        tenant_id: UUID,\n        skip: int = 0,\n        limit: int = 50,\n        is_active: Optional[bool] = None,\n    ) -> Tuple[List[User], int]:\n        # NEXORADOCS: Count query mirrors the data query for accurate pagination.\n        q = self._base_query(tenant_id)\n        if is_active is not None:\n            q = q.where(User.is_active == is_active)\n\n        count_q = select(func.count()).select_from(q.subquery())\n        total_result = await self._session.execute(count_q)\n        total: int = total_result.scalar_one()\n\n        data_q = q.offset(skip).limit(limit).order_by(User.created_at.desc())\n        result = await self._session.execute(data_q)\n        users: List[User] = list(result.scalars().all())\n        return users, total\n\n    async def get_user_by_id(\n        self,\n        *,\n        tenant_id: UUID,\n        user_id: UUID,\n    ) -> Optional[User]:\n        # NEXORADOCS: tenant_id AND user_id both required in WHERE clause.\n        result = await self._session.execute(\n            self._base_query(tenant_id).where(User.id == user_id)\n        )\n        return result.scalars().first()\n\n    async def update_user(\n        self,\n        *,\n        tenant_id: UUID,\n        user_id: UUID,\n        email: Optional[str] = None,\n        full_name: Optional[str] = None,\n        role: Optional[UserRole] = None,\n        password: Optional[str] = None,\n        is_active: Optional[bool] = None,\n    ) -> Optional[User]:\n        user = await self.get_user_by_id(tenant_id=tenant_id, user_id=user_id)\n        if user is None:\n            return None\n\n        # NEXORADOCS: Only update fields that were explicitly provided (PATCH semantics via PUT).\n        if email is not None:\n            normalized = email.lower().strip()\n            if normalized != user.email:\n                # NEXORADOCS: Prevent duplicate emails within the same tenant on update.\n                dup = await self._session.execute(\n                    self._base_query(tenant_id)\n                    .where(User.email == normalized)\n                    .where(User.id != user_id)\n                )\n                if dup.scalars().first() is not None:\n                    from fastapi import HTTPException, status\n                    raise HTTPException(\n                        status_code=status.HTTP_409_CONFLICT,\n                        detail=f\"Email '{email}' is already in use within this tenant.\",\n                    )\n            user.email = normalized\n\n        if full_name is not None:\n            user.full_name = full_name.strip()\n        if role is not None:\n            user.role = role\n        if password is not None:\n            user.hashed_password = self._hash_password(password)\n        if is_active is not None:\n            user.is_active = is_active\n\n        await self._session.commit()\n        await self._session.refresh(user)\n        return user\n\n    async def delete_user(\n        self,\n        *,\n        tenant_id: UUID,\n        user_id: UUID,\n    ) -> bool:\n        # NEXORADOCS: Soft-delete — sets is_deleted=True scoped to the tenant.\n        user = await self.get_user_by_id(tenant_id=tenant_id, user_id=user_id)\n        if user is None:\n            return False\n        user.is_deleted = True\n        user.is_active = False\n        await self._session.commit()\n        return True\n\n\n# NEXORADOCS: FastAPI dependency factory — injects an async DB session into the service.\nasync def get_user_service(\n    session: AsyncSession = Depends(get_async_session),\n) -> UserService:\n    return UserService(session=session)\n"
    },
    {
      "type": "create_file",
      "path": "backend/app/db/session.py",
      "content": "# NEXORADOCS: Async SQLAlchemy session factory for all service-layer DB access.\n# NEXORADOCS: get_async_session is used as a FastAPI dependency across all service classes.\n\nfrom typing import AsyncGenerator\n\nfrom sqlalchemy.ext.asyncio import (\n    AsyncSession,\n    async_sessionmaker,\n    create_async_engine,\n)\n\nfrom backend.app.core.config import settings\n\n_engine = create_async_engine(\n    settings.DATABASE_URL,\n    echo=settings.DB_ECHO,\n    pool_pre_ping=True,\n    pool_size=10,\n    max_overflow=20,\n)\n\n_async_session_factory: async_sessionmaker[AsyncSession] = async_sessionmaker(\n    bind=_engine,\n    class_=AsyncSession,\n    expire_on_commit=False,\n    autoflush=False,\n    autocommit=False,\n)\n\n\nasync def get_async_session() -> AsyncGenerator[AsyncSession, None]:\n    # NEXORADOCS: Yields a session per request and rolls back on unhandled exceptions.\n    async with _async_session_factory() as session:\n        try:\n            yield session\n        except Exception:\n            await session.rollback()\n            raise\n        finally:\n            await session.close()\n"
    },
    {
      "type": "create_file",
      "path": "backend/app/core/config.py",
      "content": "# NEXORADOCS: Central application configuration loaded from environment variables.\n# NEXORADOCS: All secrets must be set via env vars — never hardcoded defaults in production.\n\nfrom pydantic import BaseSettings, AnyUrl, validator\nfrom typing import Optional\n\n\nclass Settings(BaseSettings):\n    # NEXORADOCS: DATABASE_URL must use the async driver prefix (postgresql+asyncpg://).\n    DATABASE_URL: str = \"postgresql+asyncpg://nexora:nexora@localhost:5432/nexora_dev\"\n    DB_ECHO: bool = False\n    SECRET_KEY: str = \"change-me-in-production\"\n    ACCESS_TOKEN_EXPIRE_MINUTES: int = 60\n    ENVIRONMENT: str = \"development\"\n\n    @validator(\"DATABASE_URL\")\n    def validate_db_url(cls, v: str) -> str:\n        # NEXORADOCS: Enforce async driver to prevent accidental sync usage.\n        if not v.startswith(\"postgresql+asyncpg\"):\n            raise ValueError(\n                \"DATABASE_URL must use 'postgresql+asyncpg://' for async SQLAlchemy.\"\n            )\n        return v\n\n    class Config:\n        env_file = \".env\"\n        env_file_encoding = \"utf-8\"\n        case_sensitive = True\n\n\nsettings = Settings()\n"
    }
  ],
  "advice": "The UserRole enum is defined in backend/app/models/user.py and imported directly in backend/app/api/user.py — ensure circular import paths are avoided if models grow. Consider moving enums to a dedicated backend/app/models/enums.py module as the project scales. The TenantMixin uses RESTRICT on the FK ondelete policy; confirm this aligns with the tenant off-boarding strategy to avoid blocked deletes in production. The passlib[bcrypt] and sqlalchemy[asyncio] (asyncpg) packages must be present in requirements.txt."
}